on:
  push:
    paths:
      - 'config/**'
      - 'boards/**'
      - 'build.yaml'
      - '.github/workflows/**'
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
